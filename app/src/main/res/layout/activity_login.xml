<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/ic_login">

    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#1A000000" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_margin="24dp"
        android:orientation="vertical"
        android:padding="28dp"
        android:gravity="center"
        android:background="@drawable/bg_login_card">

        <ImageView
            android:id="@+id/imgLogo"
            android:layout_width="90dp"
            android:layout_height="90dp"
            android:src="@drawable/ic_log"
            android:layout_marginBottom="32dp"
            android:scaleType="fitCenter"/>

        <EditText
            android:id="@+id/edtUsername"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:hint="Tên đăng nhập"
            android:inputType="text"
            android:maxLines="1"
            android:singleLine="true"
            android:paddingLeft="16dp"
            android:paddingRight="16dp"
            android:textColor="#333333"
            android:textColorHint="#888888"
            android:background="@drawable/bg_menu_item" android:nextFocusDown="@id/edtPassword"
            android:imeOptions="actionNext"/>

        <EditText
            android:id="@+id/edtPassword"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_marginTop="16dp"
            android:hint="Mật khẩu"
            android:inputType="textPassword"
            android:maxLines="1"
            android:singleLine="true"
            android:paddingLeft="16dp"
            android:paddingRight="16dp"
            android:textColor="#333333"
            android:textColorHint="#888888"
            android:background="@drawable/bg_menu_item" android:imeOptions="actionDone"/>

        <CheckBox
            android:id="@+id/cbRemember"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Ghi nhớ đăng nhập"
            android:textColor="#555555"
            android:textSize="14sp"
            android:buttonTint="#8B5E3C" /> <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btnLogin"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginTop="24dp"
        android:text="Đăng nhập"
        android:textAllCaps="false"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:textStyle="bold"
        android:background="@drawable/bg_button_login"/>

        <TextView
            android:id="@+id/txtRegister"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="Chưa có tài khoản? Đăng ký ngay"
            android:textStyle="bold"
            android:textColor="#8B5E3C"
            android:textSize="14sp"
            android:padding="8dp"/>

    </LinearLayout>
</RelativeLayout>